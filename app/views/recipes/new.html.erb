<p>

  <h1>Create a new Recipe</h1>
  <hr>
  
</p>

<%= render 'form' %> <!--This is the code that allows us to use the code from the _form.html.erb file.
we do not have to specify anything other than 'form' because it is in the same directory as the current file.-->